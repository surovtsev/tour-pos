const tourists = (state) => state.tourists;
const printDepartures = (state) => state.printDepartures;
const loadingTourists = (state) => state.UI.loading;

export { tourists, loadingTourists, printDepartures };
